# agents.md -- 全局背景与行为准则(入库模板)

> **使用方法**:复制本文件为 `agents.md` 后再填写真实内容。
>
> ```bash
> cp agents.example.md agents.md
> ```
>
> `agents.md` 已被 `.gitignore` 忽略——它常含真实姓名、年龄、身份等个人信息,绝不入库;
> 本模板是入库的占位,保持 `xxx` 形式的假值,不要在此填写真实个人信息。
>
> 本文件由用户手工维护,是所有智能体共享的"静态背景层"(与数据库中的长期记忆互为补充)。
> 修改后需**新开会话线程**(`/new`)才生效:启动时读入并缓存进固定 system(ADR-0011)。
> 注入范围:主智能体(answer/ask 侧)全文;调研与执行子智能体经任务契约收到全文;知识库检索子智能体不注入。

## 用户背景

name:xxx
age:xx
gender:x
- 你的个人信息
- 默认使用中文交流。

## 对智能体的全局指令

- 回答简洁、诚实,不确定就明说,绝不编造。
- 每次回复以结论收尾,不加多余寒暄。

## 各智能体人设

(待补充:主智能体与三个子智能体的人设说明)
