# 长期记忆写入策略:显式命令直写,自主写入须人工确认

长期记忆由主智能体统一写入,内容限定为用户事实与偏好。写入分两条路径:用户明确要求"记住某内容"时直接写入;智能体认为某信息值得记而自主发起写入时,必须先告知用户并进入 human-in-the-loop 确认环节(基于 LangGraph 的 interrupt 机制),用户批准后才落库。这样做的目的是让长期记忆的内容始终处于用户知情与掌控之下,避免记忆库被自动流程污染;有意拒绝了"会话结束自动提取记忆"和"完全自主写入"两种方案。
