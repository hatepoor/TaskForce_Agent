# 模块 05 · 评测集与全量回归

> 状态:✅ 已完成(2026-09-14 验收:评测库 8 篇文档 9 条查询,hybrid ≥ 纯向量全部成立;全量 191 passed + ruff 全绿)
> 目标:用固定评测库证明混合检索 ≥ 纯向量(防退化),回归改造旧断言,全量测试收口。
> 共享设计见 [00-方案总览.md](00-方案总览.md);进度见 [TODO.md](TODO.md)。

## 任务清单

- [ ] 5.1 建固定评测库 `tests/fixtures/rag_eval/`:
  - 10–20 篇文档,覆盖四类查询:专有名词 / 口语化 / 关键词组合 / 同义改写
  - 每类查询配人工判定的 gold 命中集(评测基准,改动后若 gold 需更新须登记)
- [ ] 5.2 评测断言:对每类查询,`hybrid Recall@5 ≥ 纯向量 Recall@5` 且 `hybrid MRR@5 ≥ 纯向量 MRR@5`(防退化;若某类不达标,记录并回模块 03 调 M/k,不静默放行)
- [ ] 5.3 `tests/test_rag.py` 回归改造:
  - 原 `test_search_roundtrip` 的 top-1 断言**放宽为"top_k 内命中"**(RRF 重排后 top-1 可能变化)
  - 新增混合命中用例:构造"纯向量 miss、关键词路救回"的查询,断言命中
  - 新增 score 语义断言:显式标注 score 越大越相关
- [ ] 5.4 脏重建用例:upload 后立即检索能命中新文档;delete 后立即检索不再命中(懒重建窗口覆盖)
- [ ] 5.5 全量回归:`uv run pytest -q` + `uv run ruff check .` 全绿;确认 `test_kb_search.py` 零改动通过(FakeStore 注入)

## 整优化验收标准

- [ ] `uv run pytest -q` 全绿(含新增 test_bm25.py、改造后 test_rag.py、零改动 test_kb_search.py)
- [ ] `uv run ruff check .` 全绿
- [ ] 手工:上传含专名的文档,分别用"全称 / 口语化 / 同义改写"三问,命中质量 ≥ 改造前
- [ ] 评测断言 Recall@5 / MRR@5 混合 ≥ 纯向量
- [ ] 存量知识库(旧数据)首次检索即可命中,无需重建命令(懒重建覆盖)

## 学习点

| 学习点 | 可问问题 |
|---|---|
| 检索评测方法论(Recall@k / MRR@k、gold 集、防退化断言) | "怎么证明改造有效而不是感觉?" |
| 断言放宽 vs 弱化的区别(范围内命中) | "top-1 断言为什么必须放宽?" |
