# 开发期:请求带 /api 前缀,由 vite proxy 剥离后转发到后端(后端路由无此前缀)
VITE_API_BASE=/api

# 代理目标默认 http://127.0.0.1:8010(与 api/main.py 默认端口一致;项目约定:后端端口固定,不换)。
# 不要用 8000:本机 8000 是沙箱 SSH 隧道(SANDBOX_URL 指向它)。
# 需要指向别的后端实例时取消下面注释并改成目标地址:
# VITE_PROXY_TARGET=http://127.0.0.1:8010