# 生产集成收尾 开发进度

> 编号: `12`
> 依赖模块: 01~11 全部
> 最后更新: 2026-09-17

## Task 进度总览

| Task | 名称 | 状态 | 依赖 |
|------|------|------|------|
| [task1](./task1.md) | StaticFiles 挂载与形态 B 构建 | ✅ | 01~11 全部 |
| [task2](./task2.md) | 全剧本浏览器验收与文档登记 | ✅ | task1 |

## 进度日志

### 2026-09-16

- ⬜ task1~task2: 未开始

### 2026-09-17

- ✅ task1:`src/api/main.py` 追加前端产物挂载(`_DIST.is_dir()` 条件挂载 `StaticFiles(html=True)`,置于 `include_router` 之后;`pathlib`/`StaticFiles` 提到文件头避免 E402);`npm run build` 产出 dist;实测 :8010 单进程起全栈,SPA + 静态资源 + 同源 API 全 200
- ✅ task2:形态 B 全链路 headless 探针 **30/30**(见下);UI-DESIGN 九条清单逐条核对;README/`docs/dev/TODO.md` 登记完成
- 形态 B 探针覆盖:首屏渲染 / 四条 hash 路由直达刷新 / API 同源无 `/api` 前缀 / 无跨域请求 / 对话闭环(SSE 流式 + Markdown)/ 知识库列表一致 / 多选串行上传(两请求不重叠)/ doc_id 复制内联反馈 / 删除确认弹窗回显 + 取消不改数据 / 记忆列表一致 + source 徽标 + 本地过滤 + 删除弹窗回显 / Health 三态卡 + sandbox 502 按异常态呈现 / MCP 列表 + 徽标 + 表单切 transport 不丢草稿 + 必填校验不发请求 / Skills 只读 + 重启提示 / 无 emoji 骨架屏 / 无横向溢出 / 零未捕获异常
- 设置页另跑专项探针 **13/13**(Health 卡渲染需等 `/health` 走完沙箱隧道,约 4s)
- 演示剧本:① 长期记忆(自主提案→确认→记忆页可见)与 ② ask 挂起→作答续流在模块 08 探针实测通过;③ RAG 上传→提问→检索→引用在 :8010 实测通过(retriever 派发 → 下一轮汇总并引用《05-L…》来源文档,内容截断时如实说明缺口不编造);④ 沙箱执行**未跑**(用户明示当前沙箱关闭阶段);⑤ 多任务并行派发未单独跑(清单在模块 10/11 的既有验收里覆盖过,异步派发语义见 ③ 的派发确认帧)
- **验收期发现(重要,已写入 README)**:本机 `127.0.0.1:8000` 是沙箱通道(`SANDBOX_URL=http://127.0.0.1:8000`,SSH 隧道),形态 B 若也起在 8000 会自指:沙箱自检打到前端服务 → `/health` 递归挂起 → 线程池被拖死 → 整站请求变慢(起初误判为"前端在形态 B 下卡住",实为端口冲突)。形态 B 验收改用 `--port 8010`。
- 备注:临时探针脚本用后已删;`dist/` 不入库