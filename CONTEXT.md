# 智能体协作上下文

一个以 LangGraph 为核心的单用户终端智能体系统:主智能体编排任务,三个子智能体分别负责知识库检索、调研和简单任务执行,并具备知识库、记忆、Skills 与 MCP 能力。控制面在本地(CLI 与 FastAPI 双入口),执行沙箱为部署在远程服务器上的用户自建 Docker 沙箱服务。

## Language

### 会话

**会话线程**:
用户与智能体之间一段连续对话的载体,短期记忆的生命周期边界,以 thread_id 标识。
_Avoid_: 会话、对话(口语泛称)

### 智能体拓扑

**主智能体**:
Supervisor 模式下的中央调度者,接收用户任务,负责复杂任务的亲自求解与任务编排(支持并行派发),把子任务以任务契约的形式分派给子智能体;长期记忆的唯一写入者;问询的唯一发起者。
_Avoid_: 主管、大脑、总控

**子智能体**:
在主智能体之下承担某一专长的智能体,共有三个:知识库检索智能体、调研智能体、执行智能体。子智能体之间不互相调用,协作一律经主智能体路由;也不直接发起 human-in-the-loop,信息不足时在结果摘要中标注,由主智能体决定是否问询。
_Avoid_: 专家智能体、worker

**知识库检索智能体**:
只负责从知识库中检索内容并组织答案的子智能体。
_Avoid_: RAG 智能体、检索器

**调研智能体**:
通过 Web Search 在互联网上搜索并整理信息的子智能体,内部为 ReAct 工具循环(web_search 工具,循环上限 8 轮,ADR-0010)。
_Avoid_: 研究智能体、researcher

**执行智能体**:
完成简单、明确任务的子智能体,主要使用者是 Skills 与 MCP 工具,并可在执行沙箱中运行代码与读写文件。
_Avoid_: 工具智能体、worker 智能体

**任务契约**:
主智能体派发任务时随 Send 携带的自包含上下文,由任务描述、用户原话、结构化输入与输出要求四部分构成;子智能体不接触会话历史,主智能体是其唯一的信息来源。
_Avoid_: 任务描述(仅指其中一项)、prompt

**结果摘要**:
子智能体执行完毕后回传主图的结构化摘要,固定字段含结论、要点、数据、溯源与澄清项;answer 汇总完成后即被消费清除,不跨轮残留。
_Avoid_: 执行报告、返回值(口语)

### Human-in-the-loop

**单一挂起点原则**:
所有 interrupt 只发生在主图节点(问询节点与记忆确认节点),任意时刻最多一个挂起的 interrupt;子智能体不直接 interrupt。
_Avoid_: 分布式挂起

**问询**:
主智能体遇到不确定情况或需要用户输入(任务澄清、缺失参数、凭据等)时,经 interrupt 挂起并向用户提问,用户的自由文本回答作为消息回到主图继续。
_Avoid_: ask_user 工具、人工升级

**确认写入**:
主智能体自主认为某内容值得记忆时,先向用户说明并等待批准(human-in-the-loop),批准后才写入长期记忆。
_Avoid_: 自动写入、静默写入

**显式写入**:
用户明确要求记住某内容时,主智能体直接写入长期记忆,不经过确认。
_Avoid_: 强制写入

### 能力扩展

**Skill**:
存在于本地 skills/ 目录的能力包,载体为文件夹 + SKILL.md(遵循 Anthropic Skills 规范),不存入数据库。采用渐进式加载:启动时扫描其名字与描述常驻提示词,完整内容仅在调用时从本地路径读取;其附带脚本一律经执行沙箱运行。主智能体与执行智能体均可使用。
_Avoid_: 技能、插件、工具包

**MCP 服务器**:
按 Anthropic MCP 规范接入的工具服务器,由用户在本机通过 mcp_config.json 配置(含本地 stdio 进程);工具以"索引常驻、详情按需拉取"的方式暴露给智能体,单服务器失败时降级不阻塞主流程。
_Avoid_: MCP 插件

**执行沙箱**:
执行智能体运行代码与读写文件的隔离环境,由部署在用户远程服务器上的自建 Docker 沙箱服务提供,本系统仅做接入。
_Avoid_: 代码解释器、隔离区

### 记忆与背景

**短期记忆**:
单个会话线程内的对话历史与中间状态,持久化到 PostgreSQL(LangGraph checkpointer),跨程序重启可用;会话线程即其生命周期边界。
_Avoid_: 会话记忆、上下文

**长期记忆**:
跨会话线程存在的用户事实与偏好,持久化到 PostgreSQL 并支持向量化检索(LangGraph Store + pgvector);与知识库是两套相互独立的集合。写入者仅限主智能体,分显式写入与确认写入两条路径,支持查看与删除。
_Avoid_: 永久记忆、用户画像

**agents.md**:
用户手工维护的全局背景与行为准则文件,位于固定路径 ./agents.md,包含项目/用户背景、对智能体的全局指令与各智能体人设;主智能体会话线程启动时注入,调研与执行智能体经任务契约收到全文,知识库检索智能体不注入;与长期记忆数据库互为补充(手工冷背景 vs 动态热记忆)。
_Avoid_: 配置文件、系统提示词(含义不同)

**知识库**:
由用户上传的文档(TXT、Markdown、Word、PDF)构成的资料集合,平铺管理,以文档为最小管理单元;文档经解析、切块与向量化后供知识库检索智能体通过向量召回使用,与长期记忆相互独立。
_Avoid_: 文档库、语料库
